# Parent app feature stubs

Feature-first folders to be built per the roadmap (screens re-skinned when mobile Figma lands):

- `track/` — live child-bus map + ETA (Phase 4)
- `notifications/` — push + in-app feed (Phase 5)
- `requests/` — submit pickup / zone / stop change (Phase 5) → source of admin Parent Requests
- `guardians/` — manage authorized pickup guardians (Phase 5)
- `child_profile/` — route / driver / nanny view (Phase 4)
