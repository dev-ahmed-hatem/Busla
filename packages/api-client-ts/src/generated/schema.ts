/**
 * GENERATED by `pnpm --filter @busla/api-client-ts generate` (openapi-typescript).
 * DO NOT EDIT — this Phase-0 bootstrap version is overwritten on first codegen run.
 * It mirrors contracts/openapi.yaml so the package typechecks before codegen exists.
 */
export interface paths {
  "/api/v1/health/": {
    get: {
      responses: {
        200: {
          content: {
            "application/json": components["schemas"]["Health"];
          };
        };
      };
    };
  };
}

export interface components {
  schemas: {
    Health: {
      status: "ok" | "degraded";
      service: string;
      version: string;
      /** Format: date-time */
      time: string;
    };
  };
}

export type webhooks = Record<string, never>;
export interface operations extends Record<string, never> {}
